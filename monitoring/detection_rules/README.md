# Detection Rules

Planned home for rules that identify high-value changes such as Conditional Access,
privileged role, PIM, authentication policy, application, and sensitive-group changes.

Rules should be declarative, versioned, testable with `monitoring/sample_events.json`,
and should exclude the documented break-glass identity from automated actions.