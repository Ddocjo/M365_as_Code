name: Audit Monitor (Deferred)

# Audit polling is deferred because this project only implements capabilities
# through Terraform. This workflow intentionally performs no monitoring action.
on:
  workflow_dispatch: {}

jobs:
  deferred:
    runs-on: ubuntu-latest
    steps:
      - name: Report deferred capability
        run: echo "Audit polling is deferred until reliable Terraform support exists."
